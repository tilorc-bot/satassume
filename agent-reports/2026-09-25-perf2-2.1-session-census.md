# Agent report: perf round 2, item 2.1, session census (measurement only)

- **Date:** 2026-09-25
- **Status:** measurement only; no code. Numbers from the 0.3 log of `d166940`
  (`~/.cache/satassume/log-d166940.jsonl`) and the stream, with an LRU of
  `Engine._context_sessions` simulated over the logged session requests (ad
  hoc script, not committed).
- **Read this if:** you consider changing `keep_sessions` (after 1.2), or
  item 2.2 (cloning a base session).

## Sessions by reason

| reason | before 2.5 | after 2.5 | logged ms of those queries (before) | median per query |
|---|---:|---:|---:|---:|
| cone rebuild | 757 | 757 | 1,348 | 1.40 ms |
| first sight of an assumption set | 507 | 507 (444 build, 63 fail) | 323 (the 444) | 0.47 ms |
| evicted from the 16-slot LRU, set seen before | 1,210 | 143 | 76 (the 143) | 0.41 ms |
| context-free (`is_`, `ask` without assumptions) | 190 | 190 | 133 | 0.42 ms |
| **total built** | **2,664** | **1,597** | | |

1,067 of the 1,210 "evictions" were failing sets (item 2.5), which were
never stored in the LRU, so they were rebuilt on every request. Item 2.5
answers those from its memo. The 63 first builds of failing sets remain.
This leaves 143 real evictions.

## Assumption sets

- 540 distinct SymPy assumption objects in the stream; 507 distinct
  assumption formulas reach `_context_session` (7,510 requests). 63 of the
  formulas fail (Uninterpreted); 444 build a session.
- The simulation reproduces the log exactly at 16 slots: first 507,
  rebuilt 1,210, reused 5,793 (the log's 5,036 reused plus 757 that were
  reused and then replaced by a cone).

## Evictions of sets seen before, and how far back

Excluding the failing sets (the situation after 2.5):

| LRU slots | rebuilt after eviction | saved vs 16 | rebuild distance, stream queries since last use (median / p90) |
|---:|---:|---:|---|
| 16 (today) | 143 | - | 1,327 / 5,063 |
| 32 | 99 | 44 | 2,278 / 5,939 |
| 64 | 68 | 75 | 3,584 / 7,951 |
| unbounded | 0 | 143 | - |

LRU stack distance of the 5,936 re-requests (distinct other sets used in
between): 5,793 are within 16, 5,837 within 32, 5,868 within 64; the
maximum is 390. Re-use is either immediate or very far back. Nothing
clusters just past 16.

## Reading

- After 2.5, evictions are not where sessions come from: 143 of 1,597 builds,
  about 76 ms logged. At about 0.4 ms each, a 32-slot LRU would save 44 builds
  (about 0.4% of the replay) and a 64-slot LRU 75 builds (about 0.7%). That
  is too little to re-test `keep_sessions` unless 1.2 changes the per-session
  cost picture. The plan's retest condition ("if evictions dominate") is not
  met.
- Cone rebuilds dominate what remains: 757 builds and 1,348 ms logged
  (about 31% of the logged pass, search included). Whatever 1.2 does to cone
  search decides the next session question, and 2.2 (clone) only matters if
  cones survive 1.2.
- First-sight builds (444, about 7% logged) are irreducible by caching. Only
  cheaper construction (2.2, 2.3) helps them.

## Decision

Measurement only. No `keep_sessions` A/B (the plan puts it after 1.2); by
count it would not clear 5% anyway.
